import { Component } from '@angular/core';
import { FacebookForm } from '../../components/facebook-form/facebook-form';

@Component({
  selector: 'app-form-page',
  imports: [FacebookForm],
  templateUrl: './form-page.html',
})
export class FormPage { }
